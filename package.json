{
  "name": "qrcode-with-logos-and-text",
  "version": "1.0.0",
  "description": "Create a QR code with qrcode, overlay an image in the center and optionally add text to the bottom of the output",
  "main": "build/index.js",
  "typings": "build/index.d.ts",
  "scripts": {
    "build": "rimraf -rf ./build && tsc -p . -d --declarationDir build"
  },
  "dependencies": {
    "qrcode": "^1.5.3"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.5",
    "rimraf": "^5.0.7",
    "tslib": "^2.6.3",
    "typescript": "^5.4.5"
  }
}
